const NavLink = ({title, align}) => {
    return (
        <div
            style={{
            }}
            className='nav-link'
        >
            {title}    
        </div>
    )
}

export default NavLink